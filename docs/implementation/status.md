# 实施状态

> 最后更新：2026-09-08

## 总体目标

实现 [功能清单](../plans/05-feature-inventory.md) 中的全部功能。当前不缩减最终范围，按阶段持续交付。

## 当前阶段

功能实施完成，进入上线前来源扩容与长期运行验收。

## 临时隐藏与恢复计划（2026-09-08）

- 官方 API 和中转 API 当前只隐藏前台入口，保留页面、数据与实现。
- 用户已明确：后续会恢复中转 API，不得将本次隐藏视为永久下线或删除依据；恢复时间待定。
- 当前入口由 `apps/web/src/lib/site-features.ts` 中的 `API_SECTIONS_ENABLED` 统一控制。仅恢复中转 API 时应拆分开关，避免同时恢复官方 API；官方 API 的恢复尚未确定。
- 恢复时同步检查桌面导航、移动端菜单、相关页面入口、公告与站点地图。

## 已完成

- 完成竞品、数据源和收录机制调研；
- 固化产品 PRD、系统架构、数据模型、路线图和 125 项功能清单；
- 建立 npm workspaces monorepo 配置；
- 建立 PostgreSQL、Redis、MinIO 本地依赖配置；
- 建立环境变量模板和项目入口文档。
- 建立共享领域 Schema 与运行时校验；
- 建立 15 张核心 PostgreSQL 表并生成首份迁移；
- 建立采集器 SDK 与内存注册表；
- 建立确定性商品分类、风险事实抽取与置信度输出；
- 建立报价准入、异常隔离和默认最低价排序规则；
- 建立 Shop API、Kami、独角数卡的来源指纹规则；
- 建立 Next.js 前台首页和健康检查接口；
- 建立 BullMQ Worker 进程骨架；
- 完成全部 workspace 类型检查和生产构建；
- 在独立 PostgreSQL 实例执行迁移，确认创建 15 张表；
- 完成前台 `/` 与 `/api/health` 本地 HTTP 冒烟检查。
- 建立来源 URL 自动探测、商家身份解析、去重收录与启用流程；
- 建立来源健康状态机、成功调度周期和分级失败退避；
- 建立采集运行、分页上限、全量计数校验、重复商品检查与完整快照门禁；
- 建立发布代次和频道当前指针，事务内原子切换并保留上一代；
- 完成分类结果、结构化属性、报价与价格/库存变更历史的事务化发布；
- 实现 LDXP Shop API 真实采集器，支持从店铺或单商品链接反查商家；
- 实现 BullMQ 到期来源自动入队，完整抓取成功后自动发布；
- 实现 Worker CLI：`probe`、`onboard`、`crawl`、`publish`、`bootstrap`；
- 首页接入当前发布代次，展示真实有效报价、最低价、库存、风险与原站链接；
- 完成真实公开接口端到端验证：AI小店 82/82 完整采集，52 条分类报价进入当前代次；
- 完成连续两代发布验证：当前/上一代指针正确，仅价格或库存变化时追加历史。
- 实现 Kami/异次元公开目录采集器，兼容分页、隐藏商品计数差异与数值库存；
- 实现独角数卡 v1 公开 API 采集器，按 SKU 展开价格并合并自动/手工库存；
- 实现通用 HTML 兜底采集器，支持 JSON-LD Product、`_var_item` 和同站商品链接发现；
- 三种新增适配器均通过公开原站实测：Aisou 19 条、独角数卡 9 个 SKU、星星小店 14 条；
- 当前真实数据集扩展为 4 个来源、124 条不可变原始快照和 78 条当前代次报价。
- 实现 Playwright 浏览器兜底采集器，并将其标记为高成本探测层，仅在快速适配器全部失败后启动；
- 浏览器上下文默认禁用下载和 Service Worker、拦截图片/媒体/字体，并限制链接数与并发；
- 完成浏览器真实渲染冒烟：从星星小店渲染商品链接并抽取 2/2 条 JSON-LD 报价。
- 分类规则扩展到 ChatGPT Team/Go/Pro、Codex Credits、Perplexity Pro、X Premium 等 12 类标准产品；
- 商品归类仅使用标题和目录，交付方式与风险继续读取完整描述，避免长描述中的旁支产品名污染分类；
- 建立报价异常表和规则引擎，识别不合理价格、库存矛盾、低置信分类、未分类、价格突变与同类离群；
- 异常随发布事务写入；规则或人工审核消除问题后自动转为 resolved；
- 建立按来源商品 ID 持久化的分类覆盖，重新抓取生成新快照后仍保留人工判断；
- 建立审核队列服务、分类更正/排除操作和不可变审计日志；
- 建立 8 小时 HMAC HttpOnly 管理员会话、同源 POST 检查和受保护审核后台；
- 完成审核闭环冒烟：未登录跳转、密码登录、人工通过、覆盖持久化、审计入库、再发布后异常自动关闭。
- 建立来源控制面，支持查看健康度、最近运行、暂停、恢复和立即重抓；
- 建立采集运行详情页，展示完整性指标、错误信息和原始商品样本；
- 建立异常队列与解决/忽略操作，每次人工变更均写入审计日志；
- 修正异常重发布语义：同一快照的人工忽略会保留，已解决但再次出现的异常会重新打开；
- 完成后台 HTTP 验收：未登录 307、登录 303、三个受保护页面均 200、暂停/恢复均 303，最终来源保持启用且产生 2 条审计记录。
- 建立公开渠道提交表单和投稿状态页，收集店铺 URL、名称、主营、联系方式与备注；
- 投稿接口具备同源检查、蜜罐、每指纹限流、30 天 URL 去重和不可逆 HMAC 指纹，不保存原始 IP；
- Worker 建立投稿预检队列，严格拒绝非 HTTP(S)、凭据 URL、非标准端口、私网/保留 IP 和危险 DNS 解析；
- 新投稿自动执行发卡系统识别和限制 50 页的试采集，试采快照不会取代线上快照；
- 建立渠道投稿审核页，只有试采完整才能人工通过并启用来源，重试/通过/拒绝全部记审计日志；
- 建立批量分类审核，可保留各自分类、批量更正或批量排除，并在单个事务中写入持久覆盖和审计记录；
- 完成投稿端到端验收：公开提交 303、试采 82/82、状态页/审核页 200、人工通过 303；`127.0.0.1` 投稿被自动标记为 `source_url_private_address` 并拒绝；
- 本地代理使用合成 DNS 时，可显式开启仅开发用兼容开关；默认生产策略仍拒绝保留地址。
- 建立公开标准商品详情页，展示全部可比报价、原始标题/价格文本/链接/确认时间、库存、新鲜度和风险事实；
- 商品页支持关键字、交付方式、质保、账号归属、库存、共享、接码、期限筛选和默认/价格/新鲜度排序；
- 建立全站搜索、品牌分类页、商家详情页和渠道目录，覆盖标准产品、原始标题、商家名与采集健康信息；
- 建立商品维度的价格/库存变化历史表，报价身份跨发布代次稳定；
- 建立数据、排序、陈旧策略、商业关系和交易边界说明页；
- 建立报价举报、商家举报、信息修正与退出收录表单，具备同源检查、蜜罐、HMAC 指纹限流和目标存在性校验；
- 建立后台举报处理页，支持结案、驳回和隔离报价/来源，所有处理均记审计日志；
- 完成公开端 HTTP 验收：首页、商品、搜索、品牌、渠道、商家和方法说明 7 个页面均返回 200；举报创建 303、后台查看 200、结案 303，最终状态和审计记录正确。
- 原站跳转改为平台内部重定向，只按报价和日期聚合点击数，不保存原始 IP 或单次浏览记录；
- 建立公开数据健康页，区分 24 小时完整采集率、陈旧报价率、来源健康和开放异常；
- 建立产品列表、产品详情、渠道和健康的公共只读 API，返回 60 秒缓存和 5 分钟 stale-while-revalidate 头；
- 公共 API 采用数据库窗口计数的分布式匿名限流，默认每指纹每分钟 60 次；
- 完成平台能力 HTTP 验收：健康页和 4 个 API 均为 200，缓存头正确，原站跳转为 302 且点击聚合值从 0 增加到 1。
- 建立按标准产品及当前筛选条件的降价/补货提醒，支持目标价、邮箱确认和一键取消；
- 提醒创建时将验证通知写入持久化 Outbox，未确认前不会启用，确认后会取消尚未发送的验证件；
- 每次原子发布后 Worker 按同一规格报价评估活跃提醒，只在价格跨过阈值或库存从无到有时触发；
- 通知 Outbox 具备原子抢占、超时回收、指数退避、10 次失败隔离和 HMAC 签名 Webhook，生产端点必须使用 HTTPS；
- 完成提醒端到端验收：创建 303、验证 303、状态 active，价格条件触发 1/1，本地 Webhook 收到带签名载荷，Outbox 变为 sent，取消 303 后状态为 cancelled。
- 建立 S3/MinIO JSON 对象存储模块，自动创建私有桶、写入 SHA-256 元数据并限制回放对象大小；
- 每次正式采集和投稿试采会将原始分页项、采集上下文和版本写入不可变对象；对象写入失败会使采集失败，不发布缺失证据的快照；
- 采集运行保存对象 URI 和清单哈希，每条原始商品快照保存可定位到清单内条目的 URI；
- 建立仅管理员可访问的原始清单回放接口，读取后重算 SHA-256，不一致时拒绝返回；
- 完成原始证据端到端验收：独角数卡来源 9/9 完整采集，MinIO 生成对象，9 条快照均有对象 URI，管理页/回放接口为 200，响应头哈希与本地重算哈希完全一致；
- 新增对象存储依赖后使用官方 npm 安全源审计，生产依赖为 0 个已知漏洞。
- 建立官方订阅套餐、地区/渠道当前价、不可变价格历史和 ECB 汇率快照，精确价、区间价和未知价分层存储；
- 实现 Apple App Store 地区内购采集和 Google Play 公开区间/未知价采集，实测写入 9 条官网价、7 条 App Store 精确价和 1 条 Google Play 未知价；
- 根据当日一手文档建立 OpenAI、Anthropic、Google 和 SpaceXAI 官方 API 模型/价格库，支持输入、缓存、输出、批处理、多模态、免费层、限速和历史，首批 4 个厂商 13 个价格规格；
- 建立 API 中转站目录、公开模型价格、独立探测样本和事件时间线，实测 OpenRouter 423/423 和 Vercel AI Gateway 365/287 个模型/可定价模型；
- 建立用户自带 Key 的一次性模型检测，仅允许 HTTPS 公网端点、禁止跟随重定向，Key 不入库、不进 Cookie 和响应；
- 新增 `/official-prices`、`/official-api`、`/api-transit` 三个公开频道和对应公共 API，商品页增加官方价与人民币估算对照；
- 完成官方/中转频道真实端点验收：三个页面、三个 API 和商品官方价对照页均为 200；内网检测被 400 拒绝，OpenRouter 一次性目录检测返回 423 个模型；重复刷新未产生虚假价格历史。
- 实现自定义 JSON/商家 Feed、Grok/X、搜索引擎和聚合 feed 来源发现，所有发现结果只进入人工候选库；
- 实现来源/主机/平台并发租约、有限瞬态重试、自适应频率、5 分钟总截止和语义重复候选；
- 实现不可变公共 generation 快照、latest 指针、对象清单、ETag、代次差异与事务回滚；57 条报价双向回滚哈希一致；
- 实现管理员三角色 RBAC、scrypt 密码、真实操作者审计、发现工作台、规则补丁、质量报表、赞助位和人工发布请求；
- 浏览器采集拆为独立队列/进程和最小权限数据库角色；真实浏览器任务完成后由主 Worker 原子发布；
- 实现 Prometheus 指标、错误事件、任务耗时、API 分页、安全响应头、动态陈旧策略和无障碍基础；
- 实现 PostgreSQL + MinIO 校验备份与隔离恢复演练，来源/原始快照/代次计数一致；
- 官方订阅价采集改为每日全地区扫描：Apple 174 个公开商店改用页面内嵌 JSON 解析并校验最终商店码；Google 各国 `gemini.google/{国家}/subscriptions/` 官网页直接解析本币月价；OpenAI 各国网页价通过浏览器 Worker 读取定价页自身调用的匿名结算配置接口；Google Play 只记录公开区间；新增商店可用性登记表、采集检查的 HTTP 状态/最终 URL/解析条数、来源限流与重试、同名多金额保留待核验、跨渠道同金额不当作周期证据的规则；方案与实测见 [官方订阅价自动化采集方案](../research/official-price-collection-automation-2026-09-07.md)。
- [125 项功能矩阵](feature-acceptance.md) 已逐项建立实施证据。

## 上线前仍需运营验收

- 提供 xAI、搜索引擎和可选 LLM 提取密钥；
- 将真实来源从当前 4 个扩展到 30–50 个，并持续运行不少于两周；
- 对分层随机样本进行人工标注，确认价格解析准确率达到 99.5%；
- 在生产网络执行出口防火墙、对象存储前缀策略、TLS、异地备份和告警接收端配置；
- 测试 seam 获得确认后补充自动化行为回归套件。以上是规模化/生产验收条件，不是缺失的产品功能。
